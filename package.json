{
  "name": "astro-tweet-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "check": "pnpm -r prepublishOnly && pnpm -r check",
    "format": "pnpm -r format",
    "changeset:version": "pnpm changeset version && pnpm -r generate:version && git add --all",
    "changeset:release": "pnpm changeset publish"
  },
  "keywords": [],
  "author": "Sriram Thiagarajan",
  "license": "ISC",
  "engines": {
    "pnpm": "^8.10.0"
  },
  "packageManager": "pnpm@8.10.0",
  "dependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
